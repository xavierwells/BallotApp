apiVersion: v1
kind: ConfigMap
metadata:
  name: public-runtime
data:
  # Replace this with the voter-guide origin before deployment. Comma-separate
  # origins only when a separately hosted web or editorial client is approved.
  CORS_ALLOWED_ORIGINS: https://replace-before-deploying.example.org
